# R0-P1: Project Setup (Coding Phase)

## Objective

Create the complete project scaffolding for InvestaLens — all configuration files, folder structure, dependency declarations, and environment setup. No code execution required.

## Prerequisites

- Empty workspace at project root (existing docs/ folder preserved)
- Reference: `docs/ARCHITECTURE.md` for tech stack decisions

## Tasks

### 1. Initialize Next.js Project Structure

Create the following folder structure:

```
investalens/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── (auth)/             # Auth route group
│   │   │   ├── login/page.tsx
│   │   │   ├── register/page.tsx
│   │   │   └── layout.tsx
│   │   ├── (dashboard)/        # Authenticated route group
│   │   │   ├── portfolio/
│   │   │   ├── reports/
│   │   │   ├── tools/
│   │   │   ├── tax/
│   │   │   ├── settings/
│   │   │   ├── analytics/
│   │   │   └── layout.tsx
│   │   ├── api/                # API routes
│   │   │   ├── auth/[...nextauth]/route.ts
│   │   │   ├── v1/            # Public API (versioned)
│   │   │   └── analytics/     # Python function proxies
│   │   ├── layout.tsx          # Root layout
│   │   ├── page.tsx            # Landing page
│   │   └── globals.css
│   ├── components/
│   │   ├── ui/                 # shadcn/ui components
│   │   ├── charts/             # Chart components
│   │   ├── forms/              # Form components
│   │   ├── layout/             # Layout components (nav, sidebar, etc.)
│   │   └── reports/            # Report-specific components
│   ├── lib/
│   │   ├── db.ts               # Prisma client singleton
│   │   ├── auth.ts             # NextAuth configuration
│   │   ├── validators/         # Zod schemas
│   │   ├── providers/          # Data provider interfaces
│   │   ├── calculations/       # Performance, tax, CGT logic
│   │   └── utils.ts            # Utility functions
│   ├── hooks/                  # Custom React hooks
│   ├── types/                  # TypeScript type definitions
│   └── styles/                 # Additional styles if needed
├── prisma/
│   ├── schema.prisma           # Database schema
│   └── migrations/             # Generated migrations
├── api/                        # Python serverless functions (Vercel)
│   ├── analytics/
│   │   ├── optimize.py
│   │   ├── frontier.py
│   │   ├── montecarlo.py
│   │   ├── backtest.py
│   │   ├── factor.py
│   │   ├── covariance.py
│   │   ├── stress_test.py
│   │   └── validate.py
│   └── requirements.txt
├── public/                     # Static assets
├── docs/                       # Existing documentation (preserve)
├── plan/                       # Implementation plans (preserve)
├── tests/
│   ├── unit/
│   ├── integration/
│   └── e2e/
├── .env.example
├── .env.local                  # gitignored
├── .gitignore
├── next.config.ts
├── tailwind.config.ts
├── postcss.config.js
├── tsconfig.json
├── package.json
├── vercel.json
├── eslint.config.mjs
├── prettier.config.js
└── components.json             # shadcn/ui config
```

### 2. Create `package.json`

```json
{
  "name": "investalens",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "format": "prettier --write .",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:seed": "prisma db seed",
    "db:studio": "prisma studio",
    "test": "vitest",
    "test:e2e": "playwright test",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "@prisma/client": "^5.20.0",
    "next-auth": "^5.0.0-beta.25",
    "@auth/prisma-adapter": "^2.7.0",
    "zod": "^3.23.0",
    "tailwindcss": "^3.4.0",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.5.0",
    "lucide-react": "^0.460.0",
    "@radix-ui/react-dialog": "^1.1.0",
    "@radix-ui/react-dropdown-menu": "^2.1.0",
    "@radix-ui/react-select": "^2.1.0",
    "@radix-ui/react-tabs": "^1.1.0",
    "@radix-ui/react-toast": "^1.2.0",
    "@radix-ui/react-tooltip": "^1.1.0",
    "@radix-ui/react-slot": "^1.1.0",
    "recharts": "^2.13.0",
    "date-fns": "^4.1.0",
    "bcryptjs": "^2.4.3",
    "@tanstack/react-table": "^8.20.0",
    "papaparse": "^5.4.0",
    "react-dropzone": "^14.2.0",
    "sonner": "^1.7.0",
    "zustand": "^5.0.0",
    "@vercel/kv": "^2.0.0",
    "@neondatabase/serverless": "^0.10.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@types/node": "^22.0.0",
    "@types/bcryptjs": "^2.4.0",
    "@types/papaparse": "^5.3.0",
    "prisma": "^5.20.0",
    "eslint": "^9.0.0",
    "eslint-config-next": "^14.2.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "prettier": "^3.3.0",
    "prettier-plugin-tailwindcss": "^0.6.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0",
    "vitest": "^2.1.0",
    "@testing-library/react": "^16.0.0",
    "@playwright/test": "^1.48.0"
  },
  "prisma": {
    "seed": "ts-node --compiler-options {\"module\":\"CommonJS\"} prisma/seed.ts"
  }
}
```

### 3. Create `tsconfig.json`

```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

### 4. Create `next.config.ts`

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  experimental: {
    serverActions: {
      bodySizeLimit: "10mb", // For CSV file uploads
    },
  },
  images: {
    remotePatterns: [
      { protocol: "https", hostname: "lh3.googleusercontent.com" }, // Google OAuth avatars
    ],
  },
};

export default nextConfig;
```

### 5. Create `tailwind.config.ts`

```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: ["class"],
  content: [
    "./src/components/**/*.{ts,tsx}",
    "./src/app/**/*.{ts,tsx}",
  ],
  theme: {
    container: {
      center: true,
      padding: "2rem",
      screens: { "2xl": "1400px" },
    },
    extend: {
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        // InvestaLens brand colors
        success: "hsl(var(--success))",
        warning: "hsl(var(--warning))",
        gain: "hsl(142, 76%, 36%)",    // Green for positive returns
        loss: "hsl(0, 84%, 60%)",       // Red for negative returns
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
      keyframes: {
        "accordion-down": {
          from: { height: "0" },
          to: { height: "var(--radix-accordion-content-height)" },
        },
        "accordion-up": {
          from: { height: "var(--radix-accordion-content-height)" },
          to: { height: "0" },
        },
      },
      animation: {
        "accordion-down": "accordion-down 0.2s ease-out",
        "accordion-up": "accordion-up 0.2s ease-out",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
};

export default config;
```

### 6. Create `postcss.config.js`

```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

### 7. Create `components.json` (shadcn/ui)

```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.ts",
    "css": "src/app/globals.css",
    "baseColor": "slate",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  }
}
```

### 8. Create `vercel.json`

```json
{
  "functions": {
    "api/analytics/*.py": {
      "runtime": "@vercel/python@4.3.1",
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/analytics/:path*",
      "destination": "/api/analytics/:path*"
    }
  ]
}
```

### 9. Create `.env.example`

```bash
# Database (Neon Serverless PostgreSQL)
DATABASE_URL="postgresql://user:password@host/investalens?sslmode=require"
DIRECT_URL="postgresql://user:password@host/investalens?sslmode=require"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# OAuth Providers (optional)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# Market Data
YAHOO_FINANCE_API_KEY=""  # Optional, unofficial API works without key
ALPHA_VANTAGE_API_KEY=""
OPEN_EXCHANGE_RATES_APP_ID=""

# AI Importer (optional)
GOOGLE_AI_API_KEY=""  # Gemini API key from Google AI Studio

# Vercel KV (auto-populated on Vercel)
KV_URL=""
KV_REST_API_URL=""
KV_REST_API_TOKEN=""
KV_REST_API_READ_ONLY_TOKEN=""

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

### 10. Create `.gitignore`

```
# dependencies
node_modules/
.pnp
.pnp.js

# testing
coverage/

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
prisma/migrations/

# python
__pycache__/
*.pyc
.venv/
api/analytics/.venv/

# IDE
.vscode/settings.json
.idea/
```

### 11. Create `eslint.config.mjs`

```javascript
import { dirname } from "path";
import { fileURLToPath } from "url";
import { FlatCompat } from "@eslint/eslintrc";

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

const compat = new FlatCompat({ baseDirectory: __dirname });

const eslintConfig = [
  ...compat.extends("next/core-web-vitals", "next/typescript"),
  {
    rules: {
      "@typescript-eslint/no-unused-vars": ["warn", { argsIgnorePattern: "^_" }],
      "@typescript-eslint/no-explicit-any": "warn",
    },
  },
];

export default eslintConfig;
```

### 12. Create `prettier.config.js`

```javascript
/** @type {import("prettier").Config} */
module.exports = {
  semi: true,
  trailingComma: "es5",
  singleQuote: false,
  tabWidth: 2,
  plugins: ["prettier-plugin-tailwindcss"],
};
```

### 13. Create `src/lib/utils.ts`

```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}

export function formatCurrency(
  amount: number,
  currency: string = "AUD",
  locale: string = "en-AU"
): string {
  return new Intl.NumberFormat(locale, {
    style: "currency",
    currency,
    minimumFractionDigits: 2,
    maximumFractionDigits: 2,
  }).format(amount);
}

export function formatPercent(value: number, decimals: number = 2): string {
  return `${value >= 0 ? "+" : ""}${value.toFixed(decimals)}%`;
}

export function formatDate(date: Date | string, format: string = "short"): string {
  const d = typeof date === "string" ? new Date(date) : date;
  if (format === "iso") return d.toISOString().split("T")[0];
  return d.toLocaleDateString("en-AU", {
    year: "numeric",
    month: format === "long" ? "long" : "short",
    day: "numeric",
  });
}
```

### 14. Create `src/app/globals.css`

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
    --card: 0 0% 100%;
    --card-foreground: 222.2 84% 4.9%;
    --popover: 0 0% 100%;
    --popover-foreground: 222.2 84% 4.9%;
    --primary: 222.2 47.4% 11.2%;
    --primary-foreground: 210 40% 98%;
    --secondary: 210 40% 96.1%;
    --secondary-foreground: 222.2 47.4% 11.2%;
    --muted: 210 40% 96.1%;
    --muted-foreground: 215.4 16.3% 46.9%;
    --accent: 210 40% 96.1%;
    --accent-foreground: 222.2 47.4% 11.2%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 210 40% 98%;
    --border: 214.3 31.8% 91.4%;
    --input: 214.3 31.8% 91.4%;
    --ring: 222.2 84% 4.9%;
    --radius: 0.5rem;
    --success: 142 76% 36%;
    --warning: 38 92% 50%;
  }

  .dark {
    --background: 222.2 84% 4.9%;
    --foreground: 210 40% 98%;
    --card: 222.2 84% 4.9%;
    --card-foreground: 210 40% 98%;
    --popover: 222.2 84% 4.9%;
    --popover-foreground: 210 40% 98%;
    --primary: 210 40% 98%;
    --primary-foreground: 222.2 47.4% 11.2%;
    --secondary: 217.2 32.6% 17.5%;
    --secondary-foreground: 210 40% 98%;
    --muted: 217.2 32.6% 17.5%;
    --muted-foreground: 215 20.2% 65.1%;
    --accent: 217.2 32.6% 17.5%;
    --accent-foreground: 210 40% 98%;
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 210 40% 98%;
    --border: 217.2 32.6% 17.5%;
    --input: 217.2 32.6% 17.5%;
    --ring: 212.7 26.8% 83.9%;
    --success: 142 76% 46%;
    --warning: 38 92% 60%;
  }
}

@layer base {
  * {
    @apply border-border;
  }
  body {
    @apply bg-background text-foreground;
  }
}
```

### 15. Create `src/app/layout.tsx`

```tsx
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "InvestaLens — Portfolio Tracker & Optimiser",
  description:
    "Track, analyse and optimise your investment portfolio. Import from any broker, get performance reports, tax calculations, and advanced analytics.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>{children}</body>
    </html>
  );
}
```

### 16. Create `src/app/page.tsx` (Landing placeholder)

```tsx
export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center">
      <h1 className="text-4xl font-bold">InvestaLens</h1>
      <p className="mt-4 text-muted-foreground">
        Portfolio Tracker &amp; Optimiser
      </p>
    </main>
  );
}
```

### 17. Create `src/lib/db.ts`

```typescript
import { PrismaClient } from "@prisma/client";

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const db =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: process.env.NODE_ENV === "development" ? ["query"] : [],
  });

if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = db;
```

### 18. Create Prisma Schema (`prisma/schema.prisma`)

This is the initial schema — will be expanded in R1-P1a.

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

// Placeholder — full schema defined in R1-P1a
model User {
  id            String    @id @default(cuid())
  name          String?
  email         String    @unique
  emailVerified DateTime?
  image         String?
  passwordHash  String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  accounts Account[]
  sessions Session[]
}

model Account {
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@id([provider, providerAccountId])
}

model Session {
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

model VerificationToken {
  identifier String
  token      String
  expires    DateTime

  @@id([identifier, token])
}
```

### 19. Create Python Function Scaffold (`api/analytics/optimize.py`)

```python
from http.server import BaseHTTPRequestHandler
import json


class handler(BaseHTTPRequestHandler):
    def do_POST(self):
        """Portfolio optimisation endpoint — placeholder."""
        self.send_response(200)
        self.send_header("Content-Type", "application/json")
        self.end_headers()
        self.wfile.write(
            json.dumps({"status": "not_implemented", "message": "R2 feature"}).encode()
        )
```

### 20. Create `api/requirements.txt`

```
numpy>=1.26.0
scipy>=1.12.0
pandas>=2.2.0
```

### 21. Create `.devcontainer/devcontainer.json` (GitHub Codespaces)

```json
{
  "name": "InvestaLens Dev",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:22",
  "features": {
    "ghcr.io/devcontainers/features/python:1": { "version": "3.12" },
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "postCreateCommand": "npm install && npx prisma generate",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "prisma.prisma",
        "bradlc.vscode-tailwindcss",
        "ms-python.python"
      ],
      "settings": {
        "editor.defaultFormatter": "esbenp.prettier-vscode",
        "editor.formatOnSave": true,
        "[prisma]": { "editor.defaultFormatter": "Prisma.prisma" }
      }
    }
  },
  "forwardPorts": [3000, 5432],
  "portsAttributes": {
    "3000": { "label": "Next.js Dev", "onAutoForward": "openBrowser" }
  }
}
```

## Deliverables Checklist

- [ ] All files listed in folder structure created
- [ ] `package.json` with all dependencies declared
- [ ] TypeScript, Tailwind, PostCSS configuration
- [ ] Prisma schema with User/Account/Session models
- [ ] NextAuth configuration placeholder
- [ ] shadcn/ui configuration (`components.json`)
- [ ] Vercel deployment config with Python runtime
- [ ] Environment variable template
- [ ] DevContainer for GitHub Codespaces
- [ ] ESLint + Prettier configuration
- [ ] `.gitignore` covering all relevant patterns
- [ ] Root layout, landing page, and globals.css
- [ ] Python function placeholder with requirements.txt

## Notes for the Agent

- Do NOT attempt to run `npm install` or any commands — this runs on Windows without npm
- Create every file with exact contents as specified
- The Prisma schema here is minimal — it will be fully expanded in R1-P1a
- shadcn/ui components will be added in R1 (they're just files, no `npx shadcn` needed)
- All imports should use the `@/` path alias
